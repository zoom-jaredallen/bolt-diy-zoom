# ======================================
# Zoom App Configuration
# ======================================
# This file is auto-generated when you create a Zoom App via bolt.diy
# Get these values from: https://marketplace.zoom.us/develop/create

# App Information (set automatically)
# ZOOM_APP_ID=your_zoom_app_id
# ZOOM_APP_NAME=Your App Name

# Project Information (for OAuth flow)
# The projectId enables automatic OAuth token retrieval via bolt.diy
# ZOOM_PROJECT_ID=proj_xxxxxxxxxxxx

# Zoom App OAuth Credentials
# Your Zoom App Client ID (public, safe for client-side code)
VITE_ZOOM_CLIENT_ID=your_zoom_client_id_here

# OAuth Configuration
# The redirect URL is handled by bolt.diy's OAuth proxy
VITE_OAUTH_REDIRECT_URL=https://zoomvibes.j4red4llen.com/api/oauth/proxy/callback

# Token Polling URL (set automatically when using project-based OAuth)
# Use this URL to poll for OAuth tokens after Marketplace authorization
# ZOOM_TOKEN_POLLING_URL=https://zoomvibes.j4red4llen.com/api/oauth/tokens/proj_xxxxxxxxxxxx

# ======================================
# How OAuth Works (Local Test)
# ======================================
# 
# 1. Your Zoom App is created with a project-specific redirect URI
# 2. When a user clicks "Add" in Zoom Marketplace (Local Test):
#    - Zoom redirects to our OAuth callback with an authorization code
#    - bolt.diy exchanges the code for tokens using your app's credentials
#    - Tokens are stored securely on the bolt.diy server
# 3. Your app can poll the token endpoint to retrieve the tokens
# 4. Once retrieved, tokens are cleared from the server (one-time read)
#
# Use the oauth-polling utility:
# ```typescript
# import { initializeOAuth } from './lib/oauth-polling';
# const tokens = await initializeOAuth();
# ```

# ======================================
# Production Deployment
# ======================================
#
# When deploying to production:
# 1. Set up your own OAuth callback endpoint
# 2. Update VITE_OAUTH_REDIRECT_URL to your production URL
# 3. Update the redirect URIs in Zoom Marketplace settings
# 4. Store ZOOM_CLIENT_SECRET securely in your backend (never in client code!)
